services:
  morent-admin-console:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: morent-admin-console
    ports:
      - "8000:8000"
    volumes:
      - .:/morent-admin-console
    env_file:
      - .env
